{
  "name": "@modsearch/extension",
  "version": "0.0.1",
  "private": true,
  "description": "ModSearch browser extension. One TS core, three builds (Chromium, Firefox, Safari-as-app).",
  "license": "GPL-3.0-only",
  "dependencies": { "@modsearch/contract": "workspace:*" },
  "scripts": {
    "build": "echo \"TODO: bundle Chromium + Firefox builds; Safari is a separate Xcode app\"",
    "test": "node src/content/capture-core.test.js",
    "lint": "echo \"TODO: eslint\""
  }
}
