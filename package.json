{
  "name": "modsearch-app",
  "private": true,
  "version": "0.0.0",
  "description": "ModSearch public client monorepo: browser extension + desktop client + shared wire contract.",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@9",
  "scripts": {
    "generate": "pnpm -C packages/contract generate",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
